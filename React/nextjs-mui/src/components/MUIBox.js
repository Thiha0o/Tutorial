"use client";
import { Box } from "@mui/material";

export default function MUIBox() {
  return (
    <Box sx={{ padding: 2, marginTop: 1, backgroundColor: "grey.100" }}>
      This is a box with padding and background.
    </Box>
  );
}